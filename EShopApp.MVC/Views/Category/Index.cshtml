@model IEnumerable<ProductCategoryResponseModel>
<h1>Category List</h1>
<a asp-action="Create" asp-controller="Category">Add New Category</a>
<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Options</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <Td>@item.Id</Td>
                <td>@item.Name</td>
                <td>
                    <a asp-action="Detail" asp-controller="Category" asp-route-id="@item.Id">View Products</a> |
                    <a asp-action="Edit" asp-controller="Category" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Delete" asp-controller="Category" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>

        }
    </tbody>
</table>